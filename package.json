{
  "private": true,
  "name": "stews",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "developApp": "ts-node ./app/scripts/startDevelopment.ts",
    "buildApp": "ts-node ./app/scripts/buildCuratorApp.ts",
    "serveApp": "sirv pretttylights_build --port 8080 --cors --single",
    "developWebsite": "NODE_OPTIONS=--openssl-legacy-provider preact watch --cwd ./website --src ./source",
    "buildWebsite": "NODE_OPTIONS=--openssl-legacy-provider preact build --verbose --cwd ./website --src ./source --sw false --esm false",
    "serveWebsite": "sirv ./website/build --port 8080 --cors --single"
  },
  "devDependencies": {
    "@types/node": "^20.4.10",
    "cross-env": "^7.0.3",
    "preact-cli": "^3.0.0",
    "sass-loader": "10",
    "sirv-cli": "^1.0.3",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "zod": "^3.21.4"
  },
  "dependencies": {
    "liqe": "^3.6.0",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^4.1.0",
    "sass": "^1.58.0",
    "typescript": "^4.9.5"
  }
}
